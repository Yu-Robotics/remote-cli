# This must match server.port in router-data/.remote-cli-router/config.json.
ROUTER_PORT=3000
# Set these to the owner of ./router-data (run `id -u` and `id -g` on Linux).
ROUTER_UID=1000
ROUTER_GID=1000
